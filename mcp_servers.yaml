# MCP Servers Configuration
# Note: Update paths below to match your local environment
servers:
  - name: weather
    command: python
    args:
      - "../ai-learning-script/tools/simple_mcp_server.py"

  # Uncomment to enable filesystem MCP server
  # - name: filesystem
  #   command: npx
  #   args:
  #     - "-y"
  #     - "@modelcontextprotocol/server-filesystem"
  #     - "/home/han/Project"
